<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Site structure

`README.md`'s "Sections" and "Swapping in real content" describe the page's current sections, shared components, and data-file conventions — read it before adding a new section or component so you're extending the existing pattern rather than rediscovering it. Keep it updated per the PR-checklist rule below whenever a diff adds/removes/renames a section, shared component, or data file.

`README.md`'s "Typography" table documents the site's type scale by role (section heading, caption, button, etc.) — there's no shared `tailwind.config.*` scale or `Heading`/`Text` component enforcing it, so match an existing role from that table when adding new text rather than picking a size that merely looks right in isolation. Update the table if a new role gets introduced.

# Git workflow

This repo auto-deletes branches on merge — one branch per PR is the convention. Before starting new work, pull `main` and cut a fresh branch off it rather than reusing an old (already-merged) branch name.

Before raising a PR, check whether `README.md` needs updating for the changes in the diff (new scripts, setup steps, env vars, structure changes, etc.).

## "Help me raise a PR for this"

Don't run type-check/lint after every individual edit — it slows down iteration. Run it after finishing a logical chunk of work (a component, a feature) instead, and always as part of the pre-PR pass below.

Automate the routine steps below freely, but stay defensive at the two points where work actually gets lost or duplicated: run `git status` before switching or creating branches — stash or cherry-pick anything uncommitted/unmerged rather than leaving it stranded — and always check `gh pr list --head <branch>` (queries GitHub directly, so it's reliable even across sessions/clones) before creating a PR, so we never open a second PR for work already in flight.

When asked to raise a PR, run through these steps in order rather than jumping straight to `gh pr create`:

1. Check if a PR already exists for the current branch (`gh pr list --head <branch>`).
   - If an open PR already exists: skip directly to type-check → README check → commit → push (below). Don't create a new branch, and don't run `gh pr create` — pushing to the branch updates the existing PR automatically.
   - If no PR exists: continue to the next step.
2. Check if the current branch is stale or already merged (`git branch --merged main`, or the branch is behind `main` and unrelated to the requested change). If so, pull `main` and cut a fresh branch off it per the convention above — don't reuse it. If the stale branch already has commits for the current task, cherry-pick them onto the new branch first; don't leave them stranded.
3. Type-check and lint (`npx tsc --noEmit`, `npm run lint`) and fix anything that surfaces.
4. Check whether `README.md` needs updating for this diff.
5. Stage all the relevant changes and make **one commit for this round of changes** (not a separate commit per file/change) — a message describing the why, not just the what. Don't split into multiple `git commit` calls; that means multiple permission prompts for one logical unit of work. If pushing to an already-open PR, this is a new commit added on top — never amend or squash commits already pushed to the PR.
6. Push the branch (`git push -u origin <branch>`).
7. Create the PR with `gh pr create`, with a summary and test plan.

# Pre-launch checklist

Content and layout come first. Once section copy, dates, and photos are final, work through this before shipping to prod:

- [x] Open Graph + Twitter card metadata (title, description, image) — image is generated by `app/opengraph-image.tsx`, styled to match the hero section
- [x] Canonical URL in metadata
- [x] JSON-LD `Event` structured data (dates, location, organizer) — `lib/structured-data.ts`, rendered in `app/page.tsx`
- [x] Branded favicon (`app/icon.png` + `app/apple-icon.png`, source logo at `public/brand/iiee-logo.png`); default `next.svg`/`vercel.svg`/etc. scaffold placeholders removed from `public/`
- [x] OG image (`app/opengraph-image.tsx`, separate from the favicon)
- [x] Custom `app/not-found.tsx` + `app/error.tsx` (branded 404/error, auto-wrapped in `SiteNav`/`SiteFooter` via root layout)
- [ ] Final content/animation pass (fade-ins, scroll effects)
- [x] Accessibility pass — audited alt text (all good), heading hierarchy (no skipped levels), and keyboard nav; fixed mobile nav's missing Escape handler, `PhotoLightbox`'s missing focus trap/scroll lock, and removed a dead `aria-label` on `BeforeAfterSlider`'s comparison handle. Color contrast findings were reviewed and intentionally left as-is (negligible visual difference, not worth the change).

Already done: `app/robots.ts`, `app/sitemap.ts`, `metadataBase` in `app/layout.tsx`.

# Production cutover (do this once content is final)

Client contact: iieemmrgovernor@gmail.com

- [x] Invite iieemmrgovernor@gmail.com to the `iieemmr` GitHub org (github.com/orgs/iieemmr/people) — needs an org owner to do it
- [x] Decide client's Vercel access — **superseded 2026-08-31 by client decision:** the $20/mo Pro Team + Viewer-seat plan below was drawn up and ready, but the client confirmed they want to **stay on free Hobby for now**. Project stays under the personal Hobby account (`paulfuentesss-dev`); no Team, no client Vercel access, for now. This does not block cutover — Hobby supports custom domains fine. Revisit the Pro Team/Viewer-seat plan (details below) later if/when the client wants their own visibility into the account: create a new Team named for the client's org, upgrade to Pro ($20/mo for 1 included paid Owner/Member seat), transfer `iieemmr-site` into it, invite the client as a free **Viewer** seat. A genuine ownership handoff (client holding Owner) would need a second paid seat, $40/mo total, since Owner and Member are billed identically — not pursued, dev stays sole paid seat/Owner if/when this happens.
- [x] Vercel plan — **client decision 2026-08-31: stay on free Hobby for now**, accepting the known ToS gray area (Hobby is meant for personal/non-commercial use; this is a live org site) as a low-realistic-risk tradeoff. Revisit if Vercel ever flags it or if the client changes their mind.
- [x] Confirm who controls DNS for the Wix account behind `iieemmr.com` — confirmed 2026-08-31: domain registration and website hosting are separate Wix subscriptions (domain $12.90/yr renews May 22, 2027; hosting "Premium plan – Core" $84/yr renews ~May 3, 2027), **both auto-renew OFF**. Decision: keep the domain registered at Wix (no benefit to transferring registrar elsewhere), just repoint DNS to Vercel at cutover. ⚠️ Flag to client: auto-renew-off on the *domain* risks losing `iieemmr.com` if not manually renewed before May 22, 2027 — the hosting plan can safely lapse, the domain should not.
- [ ] Add `iieemmr.com` + `www.iieemmr.com` in Vercel project → Settings → Domains, get the required A/CNAME records
- [x] Pick canonical domain — decided 2026-09-01: **`iieemmr.com`** (apex), matching what's already listed as "Primary domain" in the client's Wix dashboard. `www.iieemmr.com` should still be added in Vercel so it can redirect to the apex.
- [ ] Update those records in Wix DNS — confirmed via `dig` on 2026-08-31 that `iieemmr.com` has **no MX or TXT records** (no email hosted on the domain), so there's no email risk when updating DNS
- [ ] Pick a low-traffic cutover window; old Wix site goes down at that domain the moment DNS updates
- [ ] Decide whether old Wix subpages (About/Events/Contact) need 301 redirects into the new single-page site, or if losing that SEO is acceptable
- [ ] After cutover: cancel/let lapse the Wix "Premium plan – Core" hosting subscription; keep the separate domain-registration subscription active at Wix
- [x] Update the hardcoded `BASE_URL` in `app/robots.ts`/`app/sitemap.ts` and `metadataBase` in `app/layout.tsx` from `https://iieemmr-site.vercel.app` to `https://iieemmr.com` — done 2026-09-01 on `feat/cutover-domain-metadata`, held un-merged until DNS actually points `iieemmr.com` at Vercel (merging early would make the live vercel.app URL claim `iieemmr.com` as canonical while that domain still shows the old Wix site)
- [ ] Verify `iieemmr.com` in Google Search Console (verification file already added at `public/googlee715dd38fb822485.html`, just needs the domain live) and submit the sitemap

Costs: domain renewal stays with the client at Wix ($12.90/yr, currently auto-renew off — see above); GitHub is free (public repo); Vercel is **free (Hobby)** per the client's 2026-08-31 decision — $0/mo for now. Pro ($20/mo, one included paid seat) remains the fallback if the client later wants their own Viewer access or the ToS gray area becomes a concern.
